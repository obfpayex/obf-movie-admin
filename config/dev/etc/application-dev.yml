# ===============================
# = Application specific config =
# ===============================

spring:
    profiles:
        active: dev
        include: swagger

    datasource:
        hikari:
            maximum-pool-size: 5
            pool-name: HikariPool




surveillance:
    url: "http://noutv002as.utvnet.net:8080/cam-admin-rest/admin/cam/scheduled-executed"
    interval-in-milliseconds: 900000 #15 minutes

application:
    async:
        core-pool-size: 2
        max-pool-size: 50
        queue-capacity: 10000


    foo: bar
    bar: foo
    foo-bar:
      foo: foobar
      bar: foobar
